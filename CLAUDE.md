# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Purpose

AI Memory Reader is a native macOS/iOS SwiftUI app (GPL-3.0) that serves as a dedicated viewer and editor for AI agent memory files. It auto-discovers memory files written by 8 supported AI coding assistants (Claude Code, Codex, Cursor, Gemini, Continue, GitHub Copilot, Aider, OpenClaw), renders multi-MB JSONL telemetry in chunked blocks, provides GitHub-style markdown rendering, live file watching via FSEvents, an edit mode with syntax highlighting and auto-save, full-text search, and a URL scheme plus CLI for programmatic integration by AI agents.

Supported file types: `CLAUDE.md`, `AGENTS.md`, `GEMINI.md`, JSONL session transcripts, and similar AI-assistant-written files.

## Repository Structure

```
ai-memory-reader/
├── AIMemoryReader/
│   ├── AIMemoryReader.entitlements          # macOS sandbox entitlements (Release/MAS builds only)
│   └── Sources/
│       ├── App/
│       │   └── AIMemoryReaderApp.swift      # @main entry, AppDelegate, menu commands
│       ├── Models/
│       │   ├── AISource.swift               # 8 built-in AI source definitions, auto-detection, custom sources
│       │   ├── AppState.swift               # Central @Observable state: file tree, search, URL handling, FSEvents
│       │   ├── AppTheme.swift               # .standard / .eyeCare theme enum
│       │   ├── FileNode.swift               # File tree node model, supported extension list
│       │   └── SettingsStore.swift          # iCloud (NSUbiquitousKeyValueStore) + UserDefaults settings
│       ├── Resources/
│       │   ├── Assets.xcassets/             # App icons
│       │   ├── Info.plist                   # URL scheme registration, UTType declarations
│       │   ├── PrivacyInfo.xcprivacy        # Apple privacy manifest
│       │   ├── AIMemoryReader.entitlements  # Sandbox entitlements (Release)
│       │   └── AIMemoryReader-iOS.entitlements
│       ├── Utilities/
│       │   ├── BookmarkStore.swift          # Security-scoped bookmarks for sandboxed (MAS) builds
│       │   ├── FileTreeBuilder.swift        # Recursive directory tree builder
│       │   ├── FileWatcher.swift            # FSEvents C API wrapper
│       │   ├── LocalImageProvider.swift     # Local image resolution for MarkdownUI
│       │   ├── MemoryFileMatcher.swift      # AI file type allow-list (strict filter mode)
│       │   ├── MemoryReaderTheme.swift      # Custom MarkdownUI GitHub theme
│       │   ├── PDFExporter.swift            # PDF export via WKWebView
│       │   ├── ReadableMarkdownRenderer.swift  # Markdown → HTML for PDF export
│       │   ├── SearchService.swift          # Full-text search across directory or within file
│       │   ├── SplashCodeSyntaxHighlighter.swift  # Splash integration for code blocks
│       │   ├── UpdateChecker.swift          # GitHub Releases auto-updater (skipped in MAS builds)
│       │   └── WindowChrome.swift           # macOS titlebar/toolbar styling
│       └── Views/
│           ├── ContentView.swift            # Root: NavigationSplitView (Mac) / NavigationStack (iOS)
│           ├── DetailView.swift             # File viewer + TOC (macOS)
│           ├── FileFindBar.swift            # In-page ⌘F find toolbar
│           ├── FindableReadableView.swift   # Markdown + find integration
│           ├── MarkdownEditorView.swift     # NSTextView edit mode, syntax highlight + auto-save (macOS only)
│           ├── SidebarView.swift            # AI source picker + file tree
│           ├── TOCView.swift                # Right-side table of contents
│           └── UpdateBanner.swift           # Non-blocking update notification
├── AIMemoryReader.xcodeproj/               # GENERATED by XcodeGen — do not edit directly
├── AIMemoryReader-iOS.entitlements         # iOS entitlements
├── AIMemoryReader.entitlements             # macOS entitlements (Release / MAS)
├── project.yml                             # XcodeGen spec — source of truth for Xcode project
├── aimr                                    # CLI bash script; copy to /usr/local/bin for agent use
├── llms.txt                                # Machine-readable summary for AI agents
├── PLAN.md                                 # MVP dev log (Chinese + English)
├── V2-PLAN.md                              # V2 feature plan + dev log
├── V3-PLAN.md                              # V3 feature plan
├── CLAUDE_CLOUD_MEMORY_SPEC.md             # Spec for future claude.ai cloud memory sync (unimplemented)
├── MAS_METADATA.md                         # Mac App Store metadata and copy
├── OUTREACH_DRAFTS.md                      # Marketing / outreach drafts
├── LICENSING.md                            # Dual-license notes (GPL-3.0 + commercial inquiry)
├── README.md                               # Primary English README
├── README_CN.md                            # Chinese README
├── docs/                                   # GitHub Pages static site
│   ├── index.html
│   ├── privacy.html
│   └── claude-code-memory-guide.html
├── .github/
│   └── FUNDING.yml                         # Ko-fi link
└── .gitignore                              # Excludes DerivedData, xcuserstate, *.app, PROMO_*.md
```

## Development Environment Setup

**Requirements:**
- Xcode 16.0+
- macOS 15.0+ (for running the macOS target)
- XcodeGen (for regenerating the Xcode project from `project.yml`)

**First-time setup:**

```bash
# Install XcodeGen
brew install xcodegen

# Generate the Xcode project from project.yml
xcodegen generate

# Open in Xcode
open AIMemoryReader.xcodeproj
```

Press `⌘R` in Xcode to build and run.

**After any change to `project.yml`**, you must regenerate:

```bash
xcodegen generate
```

No other setup steps are needed. There are no npm packages, no Python environments, no Makefiles, and no build scripts beyond XcodeGen.

## Key Commands

```bash
# Regenerate Xcode project after project.yml changes
xcodegen generate

# Install CLI for AI agent use
cp aimr /usr/local/bin/
chmod +x /usr/local/bin/aimr

# CLI usage
aimr open /path/to/file.md
aimr open /path/to/file.md --heading "Section Title"

# URL scheme (programmatic, for AI agents)
open "aimemoryreader://open?path=$(python3 -c "import urllib.parse; print(urllib.parse.quote('/path/to/file.md'))")"
```

**There is no test runner, no lint command, no CI/CD, and no Makefile.** All validation is manual: build in Xcode (`⌘B`), run (`⌘R`), and exercise the feature.

## Architecture Overview

The app is a two-target SwiftUI project (macOS 15.0+ and iOS 17.0+) sharing the same source tree under `AIMemoryReader/Sources/`. Platform-specific code is guarded with `#if os(macOS)` / `#if os(iOS)`.

**State management:** The `@Observable` macro (Swift Observation framework) drives the entire app. `AppState` is the central state machine, injected at the root via `.environment(appState)` and consumed in child views with `@Environment(AppState.self)`.

**File watching:** FSEvents (C API, macOS only) is wrapped in `FileWatcher.swift` and driven from `AppState`. File change events are debounced before refreshing the tree. iOS has no file watching.

**Markdown rendering:** `MarkdownUI` (gonzalezreal/swift-markdown-ui 2.4.0+) with a custom GitHub-style theme (`MemoryReaderTheme`). Code blocks are syntax-highlighted via `Splash` (JohnSundell/Splash 0.16.0+).

**Edit mode:** `MarkdownEditorView` wraps `NSTextView` (AppKit) with custom syntax highlighting and auto-save. macOS only.

**Settings:** `SettingsStore` writes to both `NSUbiquitousKeyValueStore` (iCloud, primary) and `UserDefaults` (fallback). Settings sync across devices. Security-scoped bookmark data is never written to iCloud.

**Dual distribution:**
- **Debug builds** (daily Xcode runs): no entitlements, no sandbox, no permission prompts — mirrors the GitHub release zip.
- **Release / Mac App Store builds**: full App Sandbox with `AIMemoryReader.entitlements`; requires `BookmarkStore` for home folder access.

**URL scheme / CLI integration:** The `aimemoryreader://open?path=...&heading=...` URL scheme is the programmatic API for AI agents. The `aimr` bash script wraps this scheme. Path traversal (`..`) is rejected in `AppState.handleURL`.

## Key Files

| File | Role |
|------|------|
| `project.yml` | XcodeGen spec — source of truth for both targets, SPM dependencies, entitlements, version numbers. Never edit `AIMemoryReader.xcodeproj/project.pbxproj` directly. |
| `AIMemoryReader/Sources/App/AIMemoryReaderApp.swift` | `@main` entry point, `AppDelegate` (cold-start URL queue, bookmark restoration, termination), all macOS menu commands. |
| `AIMemoryReader/Sources/Models/AppState.swift` | Central `@Observable` state: file tree, source selection, FSEvents file watching + debounce, URL scheme handling, full-text search, single-file vs. directory mode. |
| `AIMemoryReader/Sources/Models/AISource.swift` | Defines the 8 built-in AI source entries (id, name, path relative to `~`, iconName, color) and detection/custom-source logic. Add new AI tool support here. |
| `AIMemoryReader/Sources/Utilities/MemoryFileMatcher.swift` | Allow-list of known AI memory filenames for strict filter mode. Update `knownFilenames` when adding new AI tool support. |
| `AIMemoryReader/Sources/Models/SettingsStore.swift` | iCloud-first settings persistence. Keys defined in private `Key` enum. Bookmark data excluded from iCloud. |
| `AIMemoryReader/Sources/Utilities/BookmarkStore.swift` | Security-scoped bookmarks for MAS sandbox builds. Check `BookmarkStore.isSandboxed` to guard sandbox-specific code paths. |
| `AIMemoryReader/Sources/Utilities/UpdateChecker.swift` | Polls GitHub `/releases/latest` on launch; skipped in MAS builds via `guard !BookmarkStore.isSandboxed`. Semver comparison with strict integer components. |
| `AIMemoryReader/Sources/Utilities/FileWatcher.swift` | FSEvents C API wrapper. macOS only. |
| `AIMemoryReader/Sources/Views/MarkdownEditorView.swift` | NSTextView-based edit mode with syntax highlighting and auto-save. macOS only. |
| `aimr` | Standalone bash CLI — no compilation. Encodes path/heading and calls `open` with the URL scheme. |
| `llms.txt` | Machine-readable summary for AI agents. Keep updated when adding new AI source support or CLI features. |

## Code Conventions and Patterns

### Swift Strict Concurrency

Swift 6.0 strict concurrency (`SWIFT_STRICT_CONCURRENCY: complete`) is enforced. The compiler rejects violations.

- All UI-touching types must be `@MainActor`.
- All shared state crossing actor boundaries must be `Sendable` or `@unchecked Sendable` with explicit justification.
- Mark new `@Observable` classes and SwiftUI views as `@MainActor` unless there is a specific reason not to.

### State Management

- Use the `@Observable` macro, not `ObservableObject` / `@Published` / Combine.
- `AppState` is the single source of truth. Inject it at the root with `.environment(appState)`. Read it in child views with `@Environment(AppState.self) var appState`.
- Do not create additional top-level state objects without strong justification.

### Platform Guards

Use `#if os(macOS)` / `#if os(iOS)` to gate platform-specific code. macOS-only features include FSEvents, `NSTextView` edit mode, `NSOpenPanel`, `WindowChrome`, `AppDelegate`, and PDF export.

### Project File Management

- `project.yml` is the sole source of truth for the Xcode project configuration.
- Never hand-edit `AIMemoryReader.xcodeproj/project.pbxproj`.
- Run `xcodegen generate` after every `project.yml` change.
- New `.swift` files placed anywhere under `AIMemoryReader/Sources/` are auto-included by XcodeGen's directory-based source rule — no `project.yml` change needed just to add a Swift file.

### File Filtering

- `MemoryFileMatcher.isLikelyMemory(_:)` controls strict filter mode: `.md`, `.mdc`, `.jsonl`, `.ndjson` always pass; `.json`/`.yaml`/`.yml` only pass if the filename is in `knownFilenames`.
- `FileNode.supportedExtensions` is the extension allow-list used by `AISource.containsSupportedFiles`.

### Settings

- `SettingsStore` writes to both iCloud and `UserDefaults` on every set. iCloud is checked first on get.
- Security-scoped bookmark data must never be written to `SettingsStore` (device-specific opaque tokens that are meaningless on other devices).
- Add new settings keys to the private `Key` enum inside `SettingsStore`.

### Version Bumping

Bump `MARKETING_VERSION` and `CURRENT_PROJECT_VERSION` in `project.yml` under both the `AIMemoryReader` and `AIMemoryReader-iOS` targets, then run `xcodegen generate`.

## How AI Assistants Should Work in This Repo

**No tests, no CI.** Build in Xcode (`⌘B`) and run (`⌘R`) to verify changes. There is no `swift test`, no `make test`, no automated validation pipeline.

**Never edit the generated Xcode project.** Edit `project.yml`, then run `xcodegen generate`. Edits to `project.pbxproj` will be overwritten on the next generation.

**Adding a new AI tool** requires changes in two places:
1. Add an `AISource` entry to `AISource.allSources` in `AISource.swift` — include `id`, `name`, `path` (relative to `~`), `iconName`, and `color`.
2. Add any tool-specific config filenames to `MemoryFileMatcher.knownFilenames`.
3. Update `README.md` and `llms.txt` to document the new tool.

**Two builds, one codebase.** Debug = unsandboxed (no entitlements file). Release = sandboxed (MAS entitlements). Always check `BookmarkStore.isSandboxed` before using sandbox-specific paths or APIs.

**The `SettingsStore` is iCloud-first.** Custom source paths and last-selected source sync across devices. Do not route ephemeral or device-specific data (such as security-scoped bookmark blobs) through `SettingsStore`.

**FSEvents is macOS-only.** All file-watching code is inside `#if os(macOS)` guards in `AppState`. iOS has no equivalent — do not add iOS file watching without testing on device.

**Edit mode is macOS-only.** `MarkdownEditorView` wraps `NSTextView`. Do not expose edit-mode features on iOS.

**PDF export is macOS-only.** `PDFExporter` uses `WKWebView` to render markdown as HTML, then prints to PDF.

**The URL scheme is the programmatic API.** `aimemoryreader://open?path=...&heading=...` is how AI agents open files. The path is URL-encoded; path traversal (`..`) is rejected in `AppState.handleURL`. The `aimr` CLI wraps this for shell use.

**The `aimr` CLI is a bash script** — no compilation or build step needed. It encodes the path and heading with Python's `urllib.parse.quote`, then calls `open` with the URL scheme.

**Development log files** (`PLAN.md`, `V2-PLAN.md`, `V3-PLAN.md`) are historical records, partly in Chinese. They document past decisions. Do not treat them as specs to implement unless actively directed to do so.

**`CLAUDE_CLOUD_MEMORY_SPEC.md`** documents a planned but unimplemented feature for syncing claude.ai cloud memory via session cookies. Do not implement it without explicit instruction — the API endpoints are reverse-engineered and subject to change without notice.

**`MAS_METADATA.md`** contains App Store copy. Do not change the app name or bundle ID without updating both `project.yml` and `MAS_METADATA.md` in the same change.

**`llms.txt`** is a machine-readable summary for AI agents consuming the app's API. Keep it current when adding new AI source support or new CLI / URL scheme features.

**Swift strict concurrency violations will fail the build.** All new types that touch UI must be `@MainActor`. All new async work crossing actor boundaries must use `Sendable` types or be explicitly audited.

## SPM Dependencies

Declared in `project.yml` under the `packages` section and resolved by Xcode's Swift Package Manager integration:

| Package | Version | Use |
|---------|---------|-----|
| `gonzalezreal/swift-markdown-ui` | 2.4.0+ | Markdown rendering with GitHub-style theme |
| `JohnSundell/Splash` | 0.16.0+ | Code block syntax highlighting |

Do not add dependencies that require a separate package manager (npm, pip, etc.). All dependencies must be SPM packages declared in `project.yml`.

## Entitlements and Distribution

| Build Configuration | Entitlements | Sandbox | Bookmark Access |
|---------------------|-------------|---------|---------------|
| Debug | none (empty string in project.yml) | No | Direct filesystem access |
| Release / MAS | `AIMemoryReader.entitlements` | Yes | Requires `BookmarkStore` |

The `UpdateChecker` polls GitHub releases on every launch and is silently skipped in sandboxed builds (`guard !BookmarkStore.isSandboxed` at the top of the check).
